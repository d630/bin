#!/bin/sh
#
# Test against daytime and adjust my config.

f=${XDG_RUNTIME_DIR:?}/DAYLIGHT;

if
    test-daylight;
then
    : > "$f";
    exec theme light;
else
    rm -f -- "$f";
    exec theme dark;
fi;

# vim: set ts=4 sw=4 tw=0 et :
