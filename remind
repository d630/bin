#!/bin/sh
#
# Wrapper: Use taskwarrior in my 'reminder' context.

export TASKRC=${TASKRC:-$XDG_CONFIG_HOME/taskrc-life};

{
    task context reminder > /dev/null;
    task +reminder "$@";
    s=$?;
    task context none > /dev/null;
} 2> /dev/null;

exit $s;

# vim: set ts=4 sw=4 tw=0 et :
